import React from 'react';
import styles from '../styles/Title.module.scss';

function TitlePage() {
    return (
        <div>
          <h1 className={styles.title}>Welcome to Hack the North!</h1>
        </div>
    );
}

export default TitlePage;